version: '1.0'

services:
  nestjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: development
      DATABASE_HOST: sql570.main-hosting.eu
      DATABASE_DB: u488253619_medicines
      DATABASE_USER: u488253619_medicines
      DATABASE_PASSWORD: R3nat1nh0123@db
      DATABASE_PORT: 3306
